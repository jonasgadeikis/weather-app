export default {
    weatherData(state) {
        return state.weatherData;
    },

    errorMessage(state) {
        return state.errorMessage;
    },

    loadingState(state) {
        return state.loadingState;
    },
};